from django.urls import path, re_path

urlpatterns = [
    # path('novaformapagamento/', CriarFormaPagamento.as_view(), name='nova_formadepagamento'),
    # path('listarformapagamento/', ListarFormaPagamento.as_view(), name='listar_formapagamento'),
    # path('detalhesformapagamento/<slug:slug>/', DetalhesFormaPagamento.as_view(), name='detalhes_formapagamento'),
    # path('editarformapagamento/<slug:slug>/', AtualizarFormaPagamento.as_view(), name='atualizar_formapagamento'),
    # path('deletarformapagamento/<slug:slug>/', DeletarFormaPagamento.as_view(), name='deletar_formapagamento'),
    #receitas

]
